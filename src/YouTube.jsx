(function (prop = {}) {
  <style jsx>{``}</style>;
  

  return (
    <img className={
      (prop.h ? "h-" + prop.h : "") +
      (prop.class ? " " + prop.class : "")
    } src="./img/yt.svg" alt="YouTube" />
  );
});
